<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movieshow_store">

    <application
        android:name=".app.Config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SansionesListActivity" />
        <activity android:name=".activities.AlquilerPorVencer" />
        <activity android:name=".activities.AlquilerVencerActivity" />
        <activity android:name=".activities.AlquilerListActivity" />
        <activity android:name=".activities.CdEditActivity" />
        <activity android:name=".activities.CdListActivity" />
        <activity android:name=".activities.EditUserActivity" />
        <activity android:name=".activities.MenuActivity" />
        <activity android:name=".activities.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



    </application>

</manifest>